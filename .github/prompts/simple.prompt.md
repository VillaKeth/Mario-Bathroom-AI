---
name: 'Simple Prompt'
agent: agent
model: Claude Opus 4.6 (copilot)
---

Can you create a "Makefile" for this project that allows me to start all different portions of the application. Include install commands and I want a "make" command that fully starts the application.

Make sure the app uses a python virtual environment and includes all necessary dependencies. Create a requirements.txt for the project since there isn't one.

Continue to loop through starting the application until you have a full "requirements.txt" and "Makefile".

---
