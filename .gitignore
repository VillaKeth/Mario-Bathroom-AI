# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
*.egg

# Virtual environments
.venv/
gpt_sovits_env/
chatterbox_env/
f5tts_env/
venv/
*_env/

# GPT-SoVITS (cloned repo, too large to track)
gpt_sovits_repo/

# Model files (large binaries)
mario_models_new/GPT_SoVITS_Mario/
mario_models_new/*.zip
Mario_Super_Show_v2/
server/data/rvc_model/

# Generated audio / test output
voice_tests/
server/data/tts_cache/

# Test scripts (not part of main app)
server/energy_test.py
server/model_compare.py
server/model_full_compare.py
server/voice_quality_test.py

# Database runtime files
*.db-shm
*.db-wal

# Test/experiment results
server/data/ralph_refs/
server/data/ralph_results/
model_comparison/

# Logs and temp output
logs/
*.log
ralph_r*_out.txt
ralph_r*_err.txt
ralph_*_debug*.txt
ralph_overnight_*.txt
server_*.log

# Python cache
__pycache__/
*.pyc

# One-off test scripts (exclude root/server, not tests/ dir)
/test_*.py
/server/test_*.py
analyze_ralph*.py

# Cache build results
perfect_cache_results.json
cache_scores.json

# One-off utility scripts
check_cache.py
model_comparison_test.py

# Media files
mario_assets/*.mp4

# Server TTS cache (alternate location)
server/tts_cache/

# IDE / OS
.claude/
.vscode/
*.swp
*.swo
Thumbs.db
.DS_Store

# Client asset variants
client/assets/variants

config.json

quadrant.db/

# Test audio files (freesound.org downloads)
tests/*.wav
tests/*.mp3

# Superpowers brainstorm files
.superpowers/

models-v2.1.zip
server/data/qdrant_memories/.lock
server/data/qdrant_faces/.lock
server/data/qdrant_faces/.lock
server/data/qdrant_voices/.lock
screenshot_pygame_desktop2.png
client_screenshot.png
hsr_gen_log.txt
hsr_gen_err.txt
test_sprite.png
_screenshot.py

# Secrets — never commit API keys/tokens
.secrets/
.env
*.token
*_token.txt

# Large model checkpoints / repos (multi-GB — fetched by setup scripts, never commit)
fish_speech_ckpts/
fish_speech_repo/
fish_speech_env/
gpt_sovits_env/

# Qdrant runtime data (collections + lock + meta churn) — not source
server/data/qdrant_*/collection/
server/data/qdrant_*/*.lock

# Wizard draft workspace (staged uploads before character is built)
character_creator/_drafts/

# Voice fine-tune datasets (regenerable via build_voice_dataset.py; large audio)
characters/*/voice/dataset/
mario_models_new/
flux_drip.log
