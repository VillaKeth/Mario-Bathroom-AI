# === Core Client ===
websocket-client==1.8.0
sounddevice==0.4.7
numpy==1.26.4
pygame==2.6.0
opencv-python==4.10.0.84
Pillow==10.4.0

# === Optional: Person Detection (requires large PyTorch download) ===
# Uncomment if you want YOLO-based person detection:
# ultralytics>=8.2.0

# === Optional: Face Recognition (requires cmake + dlib + Visual C++ Build Tools) ===
# Uncomment if you want face-based guest identification:
# face_recognition>=1.3.0
#
# To install face_recognition on Windows:
#   1. Install Visual Studio Build Tools (C++ workload)
#   2. Install cmake: pip install cmake
#   3. Install dlib: pip install dlib
#   4. Install face_recognition: pip install face_recognition
